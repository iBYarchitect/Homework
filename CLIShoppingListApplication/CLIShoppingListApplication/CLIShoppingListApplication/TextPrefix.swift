/// A text to place before the stream of the words or before the message.
enum TextPrefix {
    /// Prefix to use in the error messages. Shows the rest of the text is an error message.
    static let error = "ERROR"
}
